# Python 3.12.11
#
# torch / torchvision / torchaudio below are CUDA 12.9 (+cu129) wheels.
# If you're on CPU-only or a different CUDA version, install the matching
# build from https://pytorch.org/get-started/locally/ first, then run
# `pip install -r requirements.txt --no-deps` for these three, or simply
# edit the version specifiers to match your platform's wheel names.
torch==2.8.0+cu129
torchvision==0.23.0+cu129
torchaudio==2.8.0+cu129
--extra-index-url https://download.pytorch.org/whl/cu129

# Detection / segmentation / classification model zoo (YOLO family)
ultralytics==8.4.60

# Embedding backbone (ConvNeXt) + ArcFace fine-tuning
timm==0.6.7
lightning==2.6.5

# Image / array processing
opencv-contrib-python==4.13.0.92
numpy==2.2.6
pillow==10.4.0
pandas==2.3.1

# Backend API
fastapi==0.136.3
uvicorn==0.49.0
python-multipart==0.0.32
PyYAML==6.0.2

# Utilities used by scripts / notebooks
tqdm==4.67.1
matplotlib==3.10.5
rapidfuzz==3.14.3
